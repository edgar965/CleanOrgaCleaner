<?xml version="1.0" encoding="UTF-8" ?>
<Shell
    x:Class="CleanOrgaCleaner.AppShell"
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:views="clr-namespace:CleanOrgaCleaner.Views"
    Shell.FlyoutBehavior="Disabled"
    Shell.NavBarIsVisible="False">

    <!-- Login Page (shown first, no tabs) -->
    <ShellContent
        Route="LoginPage"
        ContentTemplate="{DataTemplate views:LoginPage}"
        Shell.TabBarIsVisible="False" />

    <!-- Main Tab Bar (after login) -->
    <TabBar Route="MainTabs">
        <ShellContent
            Title="Heute"
            Icon="home_icon.png"
            Route="TodayPage"
            ContentTemplate="{DataTemplate views:TodayPage}" />

        <ShellContent
            Title="Chat"
            Icon="chat_icon.png"
            Route="ChatListPage"
            ContentTemplate="{DataTemplate views:ChatListPage}" />

        <ShellContent
            Title="Auftrag"
            Icon="home_icon.png"
            Route="AuftragPage"
            ContentTemplate="{DataTemplate views:AuftragPage}" />

        <ShellContent
            Title="Einstellungen"
            Icon="settings_icon.png"
            Route="SettingsPage"
            ContentTemplate="{DataTemplate views:SettingsPage}" />
    </TabBar>

</Shell>